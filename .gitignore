node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local secrets. Public Vite configuration already tracked by Git is unaffected.
.env
.env.*
!.env.example

# TS build info
tsconfig.tsbuildinfo

# Claude Code agent worktrees / local tool state
.claude/
.wrangler/
site-dist

# Release artifacts (AAB copies) stay local; Drive/Play hold the canonical ones.
outputs/

# Raw screen-capture footage from tools/capture-clips.mjs. Regenerable, and the
# usable takes come off a GPU machine or a real phone — only the finished,
# edited video is worth keeping. See docs/PROMO_VIDEO_PLAN.md.
store/clips/
